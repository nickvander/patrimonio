<?xml version="1.0" encoding="utf-8"?>
<!--
  Small (2x1) variant — compact layout.

  SIZE FLOOR (shared by both variants): the launcher converts dp to cells
  as ceil((dp + 30) / 70), so 110dp = 2 columns and 40dp = 1 row. targetCell*
  is the DEFAULT placement; min*/minResize* is how small the user may drag it.
  Either variant can be resized to the other's size — they differ only in
  where they start. Dragging past ~90dp tall also swaps in the roomy two-line
  layout (see PatrimonioWidgetProvider.layoutFor); that layout is reachable by
  resizing but is deliberately NOT its own picker entry, because the picker
  gives every row the height of the tallest widget an app offers and one
  2-row entry padded every other entry with dead space.

  previewLayout points at a dedicated *_preview layout with sample text: the
  picker renders it with NO widget data, so aiming it at the live layout drew
  an empty white card (that shipped once).

  updatePeriodMillis is 0 ON PURPOSE — the widget is app-pushed and does no
  networking, so waking it on a schedule would redraw identical cached strings.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:initialLayout="@layout/patrimonio_widget_compact"
    android:previewLayout="@layout/patrimonio_widget_preview"
    android:description="@string/widget_description_small" />
